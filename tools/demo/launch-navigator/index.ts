import { DemoSharedBase } from '../utils';
import {} from '@arepa/launch-navigator';

export class DemoSharedLaunchNavigator extends DemoSharedBase {
  testIt() {
    console.log('test launch-navigator!');
  }
}
