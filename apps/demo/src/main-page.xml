<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="add a package first" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="ns-screen-orientacion" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="ns-store-update" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="navigator" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="ns-ar" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="ns-country-code" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="ns-signalr" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="ns-timer-ranger-picker" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>

      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>